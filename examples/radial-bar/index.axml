<view class="radial-bar">
  <pie
    radius="{{radius}}"
    innerRadius="{{innerRadius}}"
    series="{{series}}"
    legend="{{legend}}"
    cascade="{{cascade}}"
    pieLabel="{{pieLabel}}"
    sidePadding="{{sidePadding}}"
    tooltip="{{tooltip}}"
    style="{{style}}"
  />
</view>