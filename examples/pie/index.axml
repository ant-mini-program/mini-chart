<view class="{{cn}}">
  <pie
    radius="{{radius}}"
    innerRadius="{{innerRadius}}"
    series="{{series}}"
    legend="{{legend}}"
    cascade="{{cascade}}"
    pieLabel="{{pieLabel}}"
    tooltip="{{tooltip}}"
    style="{{style}}"
  />
</view>
